# 🏗️ Arquitectura del Sistema - CarteraClientes

## Flujo de Autenticación

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND (React/Vite)                     │
│                       Port: 5173 (dev)                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
         ┌────────────────────────────────────────┐
         │     Pages/Login.jsx                    │
         │  - handleLogin(usuario, contrasena)   │
         └────────────────────────────────────────┘
                              ↓
         ┌────────────────────────────────────────┐
         │   Services/AuthService.js              │
         │  - loginUser(usuario, contrasena)     │
         │  - POST /api/auth/login               │
         └────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                  BACKEND (Express.js)                        │
│                      Port: 3001                              │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │        routes/auth.js - POST /api/auth/login         │   │
│  │                                                       │   │
│  │  1. Busca usuario en BD (Prisma)                    │   │
│  │  2. Verifica password con crypt() de PostgreSQL      │   │
│  │  3. Crea User model de modelsDB/User.js             │   │
│  │  4. Retorna user.toLoginResponse()                   │   │
│  └──────────────────────────────────────────────────────┘   │
│                              ↓                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │    lib/prisma.js - Singleton PrismaClient           │   │
│  │                                                       │   │
│  │  - Reutilizable en todas las rutas                  │   │
│  │  - Logging habilitado                               │   │
│  │  - Pool de conexiones                               │   │
│  └──────────────────────────────────────────────────────┘   │
│                              ↓                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │      PostgreSQL + pgcrypto                           │   │
│  │                                                       │   │
│  │  Tabla: users                                        │   │
│  │  - password_: auto-encriptada por TRIGGER           │   │
│  │  - Usa crypt() con bcrypt (gen_salt('bf', 8))       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
     ┌────────────────────────────────────────┐
     │  Respuesta JSON                        │
     │  {                                     │
     │    "id": 2,                           │
     │    "cardCode": "HOLPECA",             │
     │    "cardName": "...",                 │
     │    "tipo": "C",                       │
     │    "usuario": "user0053",             │
     │    "email": [...],                    │
     │    "notificacion": true               │
     │  }                                     │
     └────────────────────────────────────────┘
                              ↓
     ┌────────────────────────────────────────┐
     │  AuthContext.jsx                       │
     │  - auth.login(userData)                │
     │  - Guarda en sessionStorage            │
     │  - Actualiza estado global             │
     └────────────────────────────────────────┘
                              ↓
     ┌────────────────────────────────────────┐
     │  Navigate to /account                  │
     │  (Protegida por PrivateRoute)         │
     └────────────────────────────────────────┘
```

## Flujo de Obtención de Órdenes

```
┌────────────────────────────────────┐
│  Account.jsx                       │
│  useEffect -> getOrders(cardCode)  │
└────────────────────────────────────┘
              ↓
┌────────────────────────────────────┐
│  OrderService.js                   │
│  - GET /api/cartera?cardCode=...   │
└────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  Backend: routes/cartera.js                 │
│                                              │
│  1. Query: WHERE cardcode = ?               │
│  2. Prisma retorna data con BigInt & Decimal│
│  3. Crea modelo Cartera de cada registro    │
│  4. Llama cartera.toJSON() para serializar  │
│     - BigInt → string                       │
│     - Decimal → number                      │
│  5. Retorna JSON array                      │
└─────────────────────────────────────────────┘
              ↓
┌────────────────────────────────────┐
│  OrderService.parseOrdersFromDB()  │
│  - Mapea JSON a UserOrder model    │
│  - Datos ya normalizados           │
└────────────────────────────────────┘
              ↓
┌────────────────────────────────────┐
│  Component recibe UserOrder[]      │
│  - Renderiza tabla de órdenes      │
│  - Filtros y ordenamiento          │
│  - Detalles de documentos          │
└────────────────────────────────────┘
```

## Estructura de Carpetas

```
CarteraClientes/
├── src/
│   ├── Components/          (React components)
│   ├── Pages/              (React pages)
│   ├── contexts/           (React context - AuthContext)
│   ├── Layout/             (Layout components)
│   │
│   ├── Models/             (Frontend models)
│   │   ├── CartItem.js
│   │   ├── Order.js
│   │   ├── Product.js
│   │   ├── User.js
│   │   ├── UserOrder.js
│   │   └── ProductTechnicalInfo.js
│   │
│   ├── modelsDB/           (Database models - NUEVO)
│   │   ├── User.js         (Corresponde a tabla users)
│   │   ├── Cartera.js      (Corresponde a tabla cartera)
│   │   ├── Pedido.js       (Corresponde a tabla pedidos)
│   │   ├── Item.js         (Corresponde a tabla items)
│   │   └── README.md
│   │
│   ├── Services/           (API services)
│   │   ├── AuthService.js       ✅ Actualizado
│   │   ├── CartService.js       ✅ Actualizado
│   │   ├── OrderService.js      ✅ Actualizado
│   │   ├── ProductService.js
│   │   └── UserSessionState.js  ✅ Actualizado
│   │
│   ├── App.jsx
│   ├── App.css
│   ├── index.css
│   └── main.jsx
│
├── routes/                 (Backend express routes)
│   ├── auth.js            ✅ POST /api/auth/login
│   ├── cartera.js         ✅ GET /api/cartera
│   ├── pedidos.js         ✅ GET /api/pedidos
│   └── items.js           ✅ GET /api/items
│
├── lib/
│   └── prisma.js          (Singleton PrismaClient)
│
├── prisma/
│   └── schema.prisma      ✅ Actualizado con mappings
│
├── public/
│   └── data/
│       ├── cartera.json
│       ├── productdetails.json
│       └── usuarios.json
│
├── server.js              (Express server unified)
├── vite.config.js         (Vite config con proxy)
├── package.json
├── .env                   (DATABASE_URL, PORT=3001)
└── dist/                  (React build output)
```

## Modelos Base de Datos vs Frontend

```
┌─────────────────────────────────────────────────────────────┐
│                     PostgreSQL Tables                        │
├─────────────────────────────────────────────────────────────┤
│  users  │ cartera │ pedidos │ items                         │
└─────────────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────────────┐
│  Prisma Schema + Mappings (prisma/schema.prisma)            │
├─────────────────────────────────────────────────────────────┤
│  @map("card_code"), @map("password_"), etc.                 │
└─────────────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────────────┐
│  modelsDB/ (Backend Models - Serialización)                 │
├─────────────────────────────────────────────────────────────┤
│  User.js, Cartera.js, Pedido.js, Item.js                   │
│  - Métodos toJSON(), toLoginResponse(), etc.               │
│  - Manejo de BigInt, Decimal, tipos especiales             │
└─────────────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────────────┐
│  JSON Response (Normalizado)                                │
├─────────────────────────────────────────────────────────────┤
│  - BigInt como string                                       │
│  - Decimal como number                                      │
│  - Fechas como ISO strings                                  │
└─────────────────────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────────────┐
│  Frontend Models (src/Models/)                              │
├─────────────────────────────────────────────────────────────┤
│  UserOrder, CartItem, Product, etc.                         │
│  - Parsean JSON normalizado del backend                     │
│  - Datos listos para UI                                     │
└─────────────────────────────────────────────────────────────┘
```

## Flujo de Verificación de Contraseña

```
Frontend                          Backend                       PostgreSQL
───────                           ───────                       ──────────

loginUser()
  │
  └─→ POST /api/auth/login
      { usuario, contrasena }
                │
                └─→ routes/auth.js
                    │
                    ├─→ Busca en BD
                    │   SELECT * FROM users
                    │   WHERE usuario = ?
                    │
                    ├─→ Obtiene password hash
                    │   (auto-encriptada por trigger)
                    │
                    └─→ Verifica con PostgreSQL crypt()
                        SELECT crypt('plaintext', '$2b$...')
                               = '$2b$...' as password_valid
                                    │
                                    └─→ PostgreSQL: Valida bcrypt hash
                                        │
                                        ├─ Hash correcto ✅
                                        │   → Login exitoso
                                        │
                                        └─ Hash incorrecto ❌
                                            → 401 Unauthorized
```

## Características Implementadas

### Backend (Express + Prisma)
- ✅ Autenticación con bcrypt (verificación PostgreSQL)
- ✅ Serialización correcta (BigInt, Decimal)
- ✅ Modelos DB específicos
- ✅ Rutas RESTful con filtros
- ✅ Validación de conexión BD
- ✅ Logging de queries

### Frontend (React + Vite)
- ✅ AuthContext global
- ✅ Servicios de API tipados
- ✅ Manejo de sesión persistente
- ✅ Rutas protegidas
- ✅ Manejo de errores

### DevOps
- ✅ Comando unificado: `npm start`
- ✅ Proxy Vite para desarrollo
- ✅ Variables de entorno
- ✅ PostgreSQL con triggers

## Comandos Útiles

```bash
# Desarrollo (hot reload frontend + backend)
npm run dev

# Producción (build + run)
npm start

# Build frontend solamente
npm run build

# Regenerar Prisma Client
npx prisma generate

# Ver esquema de BD
npx prisma studio
```

## Estado Actual: ✅ TOTALMENTE FUNCIONAL

- ✅ Login con bcrypt verificación PostgreSQL
- ✅ Obtención de órdenes y pedidos
- ✅ Modelos sincronizados con BD
- ✅ Servicios sin campos innecesarios
- ✅ Serialización correcta JSON
- ✅ Sistema unificado ejecutable
